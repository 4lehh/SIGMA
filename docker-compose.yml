version: '3.8'

services:
  server:
    build: ./server
    container_name: sigma_server
    volumes:
      - ./server:/app
    ports:
      - "9001:9001"
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    cap_add:
      - NET_ADMIN # permiso necesario para red

  sensor:
    build: ./sensor
    container_name: sigma_sensor
    volumes:
      - ./sensor:/app
    depends_on:
      - server
      - dashboard
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
    cap_add:
      - NET_ADMIN # permiso necesario para red

  dashboard:
    build: ./dashboard
    container_name: sigma_dashboard
    volumes:
      - ./dashboard:/app
    ports:
      - "8080:8080"    # Dashboard web
      - "8765:8765"    # WebSocket
      - "9002:9002/udp"  # UDP
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
    cap_add:
      - NET_ADMIN # permiso necesario para red
